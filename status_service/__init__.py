"""这个文件表示 `status_service` 是一个 Python 包。

初学者说明：
- 只要一个文件夹里有 `__init__.py`，Python 就会把这个文件夹当作“包”。
- 包的作用是把多份代码文件组织在一起，方便我们用 `import status_service...` 的方式导入。
"""

# `__all__` 用来告诉别人：从这个包里推荐公开使用哪些变量。
# 这里我们只公开 `__version__`，也就是当前服务的版本号。
__all__ = ["__version__"]

# `__version__` 是一个常见约定，表示当前代码包的版本。
# 后续如果接口或功能变化，可以把这里改成新的版本号。
__version__ = "0.1.0"
